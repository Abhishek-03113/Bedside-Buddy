# Notes

## Repo scope

In **this repository** we only work on:

- `packages/shared` — shared types / protocol
- `packages/desktop` — Electron shell, main process, TV renderer

`packages/remote` (phone PWA) is **out of scope** here. It may exist as scaffolding for the monorepo layout, but do not implement, refine, or track remote UI work in this repo.
